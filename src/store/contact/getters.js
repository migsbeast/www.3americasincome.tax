export function contactName (state) {
  return state.contactData.name
}

export function contactEmail (state) {
  return state.contactData.email
}

export function contactPhone (state) {
  return state.contactData.phone
}

export function contactMessage (state) {
  return state.contactData.message
}
